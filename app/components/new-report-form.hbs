<FormBuilder @for={{this.formData}} @action={{fn this.send}} as |f|>

  {{#each this.sortedFields as |field|}}
    <f.input @attr={{field.key}} @as={{field.fieldType}} @label={{field.label}} />
    <hr>
  {{/each}}

  <f.submit />

</FormBuilder>

{{yield}}
